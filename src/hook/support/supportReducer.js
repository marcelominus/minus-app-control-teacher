// import { PETITION_LOGIN, PETITION_EXIT_LOGIN } from "../../utils/index";
//
//Zona de SWITCH
export default (state, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
